twig:
    file_name_pattern: '*.twig'
    form_themes:
        - theme.html.twig
    globals:
        locale: "%locale%"
        version: "%version%"
        title: "%title%"
        mercure_url: '%mercure_url%'
        appService: '@App\Service\ApplicationSettingsService'

when@test:
    twig:
        strict_variables: true
