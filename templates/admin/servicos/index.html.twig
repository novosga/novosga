{% extends "admin.html.twig" %}

{% block adminContent %}
    <div class="row">
        <div class="col-8">
            <h1>
                {{ 'admin.services.title'|trans }}
                <small>
                    {{ 'admin.services.subtitle'|trans }}
                </small>
            </h1>
        </div>
        <div class="col-4 text-right">
            <a href="{{ path('admin_servicos_new') }}" class="btn btn-primary">
                <i class="fa fa-plus"></i>
                {{ 'admin.services.button.new'|trans }}
            </a>
        </div>
    </div>
    
    <hr>
        
    {% include "flashMessages.html.twig" %}
    
    <table class="table table-bordered">
        <thead class="thead-light">
            <tr>
                <th>#</th>
                <th>{{ 'admin.services.field.name'|trans }}</th>
                <th>{{ 'admin.services.field.weight'|trans }}</th>
                <th>{{ 'label.status'|trans }}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for servico in servicos %}
                <tr>
                    <td>
                        {{ servico.id }}
                    </td>
                    <td>
                        {{ servico.nome }}
                    </td>
                    <td>
                        {{ servico.peso }}
                    </td>
                    <td>
                        {% if servico.ativo %}
                            {{ 'label.enabled'|trans }}
                        {% else %}
                            {{ 'label.disabled'|trans }}
                        {% endif %}
                    </td>
                    <td class="text-center">
                        <a href="{{ path('admin_servicos_edit', { id: servico.id }) }}" class="btn btn-secondary">
                            <i class="fa fa-edit"></i>
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}